(ns ae.operations.federator-operations
  (:require [clojure.core.async :as a]
            [ae.kernel :as k]
            [ae.keywords :as kw]))

(defn create-run-federation
  [env]
  )

(defn create-federator-operations
  [env]
  (create-run-federation env))
